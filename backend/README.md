# Recon Dashboard - Backend

## Setup

```bash
cd backend
python -m venv venv
venv\Scripts\activate   # Windows
pip install -r requirements.txt
```

## Run

```bash
uvicorn main:app --reload
```

API available at `http://localhost:8000`. Docs at `/docs`.
